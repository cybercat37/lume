println "HTTP config sugar"

let client = http {
  baseUrl: "http://api.example.test",
  headers: ["x-client": "axom"],
  timeout: 1200,
  retry: 2
}

let request = client |> get("/health")
print request
