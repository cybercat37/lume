println "HTTP config sugar with retry"

let client = http {
  baseUrl: "http://api.example.test",
  headers: ["x-client": "axom"],
  timeoutMs: 1500,
  retry: 2
}

let request = client |> get("/users/1")
print request
